#include <stdio.h>

int main() {
    int i;

    // Mencetak deret angka dari 100 hingga 55 dengan interval 5
    for (i = 100; i >= 55; i -= 5) {
        printf("%d ", i);
    }

    return 0;
}
